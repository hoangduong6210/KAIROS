# Experiment configurations

`benchmark-v1.json` is the active frozen benchmark configuration. Future
immutable, reviewed configurations belong here and must be referenced by a
versioned protocol and evidence record. Historical parameters embedded in
archived scripts remain historical and are not approved defaults.
