# KAIROS final conference artifact

`KAIROS_FINAL.pdf` is the preserved final conference paper. Its SHA-256 is
`184f40f4e6c4a22555f7ae568bbeb5f7d2105a80e495c71b09bc9e8e90eea9e0`.

This snapshot is intentionally PDF-only. Its former venue-specific LaTeX
template and public source bundle were removed on 2026-08-19 under explicit
project-owner authorization. The exception is recorded in `artifact.toml`;
`checksums.sha256` now inventories only the retained canonical artifact.

This is a final **conference artifact**, not an admitted current scientific
paper release. The evidence release remains `UNRELEASED`; therefore this
directory intentionally has no `results.lock.yaml`, and `paper/CURRENT` remains
`UNRELEASED`. Future manuscript work belongs in `paper/current-state/` and must
not be represented as a rebuild of this PDF. Do not modify this snapshot in place;
any later correction requires a new artifact identifier and checksum.
